# Rice sync plan

- [x] Snapshot live dotfiles into `linux-setup/dotfiles`:
  - `~/.config/hypr`
  - `~/.config/waybar`
  - `~/.config/themes`
  - `~/.config/ghostty`
  - `~/.config/rofi`
  - `~/.config/swaync`
  - `~/.config/gtk-3.0`
  - `~/.config/gtk-4.0`
  - `~/.config/hyprland-mimeapps.list`
  - `~/.tmux.conf`
  - `~/.zshrc`
  - `~/.zprofile`
  - `~/.gtkrc-2.0`
  - `~/.local/bin/waybar-set`
  - `~/.local/bin/theme-apply`
- [x] Vendor `~/Pictures/anime` under `linux-setup/Pictures/anime`, excluding its nested `.git`.
- [x] Update docs so install/restore covers `waybar-set`, `wallpaper-set`, `theme`, `reload-tabs`, tmux config, and the bundled anime wallpapers.
- [x] Check for secrets, machine-local junk, broken symlinks, nested git metadata, and accidental cache/state files.
- [x] Verify the repo snapshot:
  - compare key live paths against copied files
  - run syntax/smoke checks where available
  - inspect `git diff --stat` and `git status`
- [x] Commit and push `main` to `origin`.
